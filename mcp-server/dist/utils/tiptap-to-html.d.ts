/**
 * Convert TipTap JSON content to HTML string (fragment only, no styles)
 */
export declare function convertToHtml(content: Record<string, unknown>): string;
/**
 * Convert TipTap JSON content to a full styled HTML document
 * suitable for printing / PDF export via browser.
 */
export declare function convertToStyledHtml(content: Record<string, unknown>, title: string): string;
//# sourceMappingURL=tiptap-to-html.d.ts.map
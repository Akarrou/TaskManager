# 4. Guide de Démarrage Rapide

## 📋 Prérequis

- Node.js (version X.Y)
- Java (version X.Y)
- Docker

## 🚀 Lancement en Développement

1.  Clonez le repository : `git clone ...`
2.  Installez les dépendances : `pnpm install`
3.  Lancez la base de données : `docker-compose up -d database`
4.  Lancez le backend : `./mvnw spring-boot:run`
5.  Lancez le frontend : `pnpm run start my-app`

## ✅ Vérification

L'application doit être accessible sur `http://localhost:4200`.
description:
globs:
alwaysApply: false

---

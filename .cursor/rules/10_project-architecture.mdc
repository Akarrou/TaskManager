---
description: Architecture du projet TaskManager - source unique de vérité pour la structure
globs: ["**/*"]
alwaysApply: false
---

# 🏛️ Architecture du Projet TaskManager

## Rôle & Contexte

Ce fichier est la source unique de vérité pour l’architecture, l’arborescence et les patterns structurels du projet TaskManager. Il doit être référencé par toutes les autres règles Cursor.

---

## 📐 Arborescence du Projet

Le projet est une application Angular autonome située dans le répertoire `TaskManager-Angular`.

```text
TaskManager-Angular/
  src/
    app/
      core/
        guards/                   # Guards de route (ex: AuthGuard)
        services/                 # Services Core (AuthService, SupabaseService, etc.)
      features/                   # Modules fonctionnels (Feature Modules)
        auth/                     # Authentification (login, etc.)
        dashboard/                # Dashboard principal
        projects/                 # Gestion des projets
        tasks/                    # Gestion des tâches (création, édition, vue)
          task-form/
          task-tree/
      shared/
        components/               # Composants partagés et réutilisables (UI)
          confirm-dialog/
          generic-kanban/
          header-nav/
          kanban-card/
        models/                   # Modèles de données partagés (déplacer les modèles ici)
        pipes/                    # Pipes partagés
        store/                    # Logique NGRX (si partagée)
      store/                      # Configuration principale du store NGRX
        reducers/
      assets/
        images/
      environments/               # Fichiers d'environnement (dev, prod)
      styles/                     # Styles globaux (variables, mixins SCSS)
    main.ts                       # Point d'entrée principal de l'application
    styles.scss                   # Fichier de styles global
  angular.json                    # Configuration du projet Angular
  package.json                    # Dépendances et scripts
  tailwind.config.js              # Configuration de Tailwind CSS
```

---

## 🧑‍💻 Patterns Structurels

- **Organisation par fonctionnalités (`features`)** : Le code est organisé en modules basés sur les fonctionnalités métier (ex: `projects`, `tasks`). Chaque "feature" est auto-contenue autant que possible (ses propres composants, services, et état NGRX).

- **`core` vs `shared`** :

  - `core/` : Contient les services et guards qui ne doivent être instanciés qu'une seule fois dans toute l'application (ex: `AuthService`).
  - `shared/` : Contient les composants, pipes, et directives réutilisables à travers les différents modules fonctionnels.

- **Gestion d'état avec NgRx** :

  - Chaque module fonctionnel (`feature`) possède son propre "slice" de state dans le store NgRx (`actions`, `reducer`, `selectors`, `effects`).
  - Un store principal (`src/app/store`) assemble les différents reducers.

- **Convention de nommage** :
  - Fichiers : `kebab-case` (ex: `task-form.component.ts`).
  - Classes : `PascalCase` (ex: `TaskFormComponent`).
  - Sélecteurs de composants : `app-kebab-case` (ex: `app-task-form`).

---

## ⚠️ RÈGLE DE MISE À JOUR : Check-list de sortie

**AVANT de finaliser une tâche impliquant une modification de la structure du projet** (création/suppression/déplacement de modules, services critiques), l'IA doit :

1.  ✅ **Vérifier la cohérence** : S'assurer que les changements respectent les patterns architecturaux décrits ici.
2.  ✍️ **Proposer une mise à jour** : Identifier les sections de ce document devenues obsolètes.
3.  📋 **Générer un plan de mise à jour** : Proposer les modifications à appliquer à ce fichier pour qu'il reflète le nouvel état du projet.

---

## 📝 Bonnes pratiques

- **Synchronisation automatique obligatoire** : Ce document DOIT être mis à jour à chaque modification structurelle
- **Source unique de vérité** : Utiliser ce document comme référence absolue pour l'architecture TaskManager
- **Documentation des patterns** : Chaque nouveau pattern doit être documenté avec ses exemples
- **Cohérence architecturale** : Valider que chaque ajout respecte les conventions existantes
- **Traçabilité des changements** : Maintenir un historique des évolutions structurelles

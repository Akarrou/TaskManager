# ===================================================================
# Edge Functions - Environment Variables Template
# ===================================================================
# Copy to .env and fill in the values.
# These are used by Supabase Edge Functions (Deno runtime).
# ===================================================================

# Google Calendar OAuth (Required for Google Calendar integration)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:4010/profile/google-callback
TOKEN_ENCRYPTION_KEY=
